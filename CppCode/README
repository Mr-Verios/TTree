This is the C++ code
Note that unlike the Python Code, this code does not build the entire tree, only the portions required to determine size
The input to this program must be a csv where the columns represent prefix bit string, length, and value
